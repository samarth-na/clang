#include <stdio.h>
#define MAXSIZE 10

int main() {

    int rear, front;
    rear = 10;

    if (rear >= MAXSIZE - 1) {
        printf("\r Queue is full");
        return 0;
    }
    if (front == -1) {
        printf("something");
        printf("something else"

    } else {
        rear++;
        printf("else something");
    }
}
