#include <stdio.h>

ind main() {
    int a[] = {1, 2, 3, 4};
    int non;
    printf("%d  ,e of non \n", non);
    for (int i = 0; i <= 4; i++) {
        printf("%d = %d \n huftf \n", non, a[i]);
    }
}
