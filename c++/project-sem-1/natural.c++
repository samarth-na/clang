#include <iostream>

int main() {
  int a;
  std::cin >> a;
  if (a > -1) {
    std::cout << "yes it is an natural no." << std::endl;
  } else {
    std::cout << "no it is not an natural no." << std::endl;
  }
}
